
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/springboot_errorHandling?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#        show_sql: true
        format_sql: true
    open-in-view: false
#        default_batch_fetch_size: 100 # 기본 페이징하는 숫자로

logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace
#    org.hibernate.orm.jdbc.bind: trace

